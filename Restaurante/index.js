import { registerRootComponent } from 'expo';

// Trago o componente que reúne os dados compartilhados e a navegação do aplicativo.
import App from './App.jsx';

// Indico ao Expo qual componente deve aparecer quando o aplicativo iniciar.
registerRootComponent(App);
